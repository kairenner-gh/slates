name: Centralize Logs With Grafana Loki
description: Ship logs from every Docker container and system service into Loki, query them with LogQL, and build dashboards that correlate logs with metrics — so you can actually debug problems across your stack.
type: Article
